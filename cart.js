// cart.js// Получаем ссылку на контейнер для корзиныconst cartItems = document.getElementById('cart-items');const cartTotal = document.getElementById('cart-total');// Функция для загрузки элементов корзиныfunction loadCartItems() {    // Ваша логика для загрузки элементов корзины, например, из Local Storage    // Пример:    // const cartData = JSON.parse(localStorage.getItem('cartData'));    // if (cartData) {    //     cartData.forEach(item => {    //         const newItem = document.createElement('li');    //         newItem.textContent = `${item.name} - $${item.price.toFixed(2)}`;    //         cartItems.appendChild(newItem);    //         updateCartTotal(item.price);    //     });    // }}// Функция для обновления общей стоимости корзиныfunction updateCartTotal(price) {    const currentTotal = parseFloat(cartTotal.textContent);    cartTotal.textContent = (currentTotal + price).toFixed(2);}// Загрузка элементов корзины при загрузке страницыloadCartItems();